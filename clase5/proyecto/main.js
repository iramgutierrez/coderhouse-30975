const moment = require('moment')

console.log(moment().format('YYYY-MM-DD'))

return

const suma = (a, b) => {
  return a + b
}

const resultado = suma(1, 2)
const random = Math.round(Math.random())

const nums = [1, 4, 19, 5, 4, 2, 1, 19, 14]

const numsObj = {
  1: 2,
  4: 2,
  19: 2,
  5: 1,
  2: 1,
  14: 1
}

console.log({ resultado, random })